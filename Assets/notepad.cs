using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class notepad : MonoBehaviour
{
    public static notepad np;

    // Start is called before the first frame update
    void Awake()
    {
       np = this; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
